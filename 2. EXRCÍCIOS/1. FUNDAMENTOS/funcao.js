function imprimirSoma(a, b){
    console.log(a + b);
}

imprimirSoma(2, 3);
imprimirSoma(5) // o segundo valor será undefined, ou seja, valor atribuído automáticamente a variável que não foi inicializada.